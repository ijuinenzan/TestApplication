<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="com.example.ijuin.testapplication.utils.MyUtils"/>
        <variable name="chatMessage" type="com.example.ijuin.testapplication.models.MessageItemModel"/>
        <import type="android.view.View"/>
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout

            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_small"
            android:visibility="@{chatMessage.isMine ? View.GONE : View.VISIBLE}"
            android:orientation="horizontal">


            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/left_node_bg">

                <TextView
                    style="@style/textview_message_left"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="@android:color/holo_orange_dark"
                    android:visibility='@{chatMessage.type.equals(MyUtils.TEXT_TYPE)? View.VISIBLE: View.GONE}'
                    android:text="@={chatMessage.message}"/>

                <VideoView
                    android:id="@+id/videoview_message_left"
                    android:layout_width="200dp"
                    android:visibility='@{chatMessage.type.equals(MyUtils.VIDEO_TYPE)? View.VISIBLE: View.GONE}'
                    android:layout_height="200dp" />

                <include
                    android:id="@+id/audio_message_left"
                    layout="@layout/layout_audio"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility='@{chatMessage.type.equals(MyUtils.AUDIO_TYPE)? View.VISIBLE: View.GONE}'/>

                <ImageButton
                    android:id="@+id/imgBtn_location_message_left"
                    android:layout_width="200dp"
                    android:layout_height="200dp"
                    android:background="@drawable/ic_play_48dp"
                    android:visibility='@{chatMessage.type.equals(MyUtils.LOCATION_TYPE)? View.VISIBLE: View.GONE}'/>
            </LinearLayout>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/margin_average"
                android:layout_marginTop="@dimen/margin_average"
                android:text="@{chatMessage.timeStamp}"
                style="@style/info_textviews"/>

        </LinearLayout>


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:visibility="@{chatMessage.isMine ? View.VISIBLE: View.GONE}"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_average"
                android:background="@drawable/right_node_bg"
                android:orientation="horizontal">



                <TextView
                    style="@style/textview_message_right"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="@android:color/holo_orange_dark"
                    android:visibility='@{chatMessage.type.equals(MyUtils.TEXT_TYPE)? View.VISIBLE: View.GONE}'
                    android:text="@={chatMessage.message}"/>

                <VideoView
                    android:id="@+id/videoview_message_right"
                    android:layout_width="200dp"
                    android:visibility='@{chatMessage.type.equals(MyUtils.VIDEO_TYPE)? View.VISIBLE: View.GONE}'

                    android:layout_height="200dp" />

                <include
                    android:id="@+id/audio_message_right"
                    layout="@layout/layout_audio"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility='@{chatMessage.type.equals(MyUtils.AUDIO_TYPE)? View.VISIBLE: View.GONE}'/>

                <ImageButton
                    android:id="@+id/imgBtn_location_message_right"
                    android:layout_width="200dp"
                    android:layout_height="200dp"
                    android:background="@drawable/ic_play_48dp"
                    app:location="@{chatMessage.message}"
                    android:visibility='@{chatMessage.type.equals(MyUtils.LOCATION_TYPE)? View.VISIBLE: View.GONE}'/>
            </LinearLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_large"
                android:layout_marginRight="@dimen/margin_average"
                android:text="@={chatMessage.timeStamp}"
                style="@style/info_textviews"/>



        </LinearLayout>

    </LinearLayout>
</layout>
